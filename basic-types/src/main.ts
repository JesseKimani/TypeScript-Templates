let countryName: string = "Kenya";
let userId: string | number = "Ke254"; // Union types allow us to declare more than one data type
let year: number = 2024;
let isLeap: boolean = true;
let population: any = "50 million";

const sum  = (a: number, b: number) => {
  return (a + b)
}

